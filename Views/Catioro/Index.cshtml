@model IEnumerable<Catioro>

@if(@Model.Count() > 0){
    <table class="table">
        <tr>
            <th>@Html.DisplayNameFor(m => m.Nome)</th>
            <th>@Html.DisplayNameFor(m => m.Idade)</th>
            <th>@Html.DisplayNameFor(m => m.Idade)</th>
        </tr>
        @foreach (var c in Model)
        {
            <tr>
                <td>@Html.ActionLink(@c.Nome, "View", new { id = c.Id })</td>
                <td>@c.Idade anos</td>
                <td>@c.Raca</td>
            </tr>
        }
    </table>
}else{
    <p>não há catioros. comece cadastrando catioros.</p>
}

@Html.ActionLink("🐶cadastrar catioro", "Create")
